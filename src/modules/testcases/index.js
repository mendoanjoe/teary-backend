const core = require('./testcases.core');
const routes = require('./testcases.routes');
const storage = require('./testcases.storage');
const validation = require('./testcases.validation');

module.exports = {
  core,
  routes,
  storage,
  validation,
};
