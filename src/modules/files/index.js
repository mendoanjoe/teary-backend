const core = require('./files.core');
const errors = require('./files.errors');
const routes = require('./files.routes');
const validation = require('./files.validation');

module.exports = {
  core,
  errors,
  routes,
  validation,
};
