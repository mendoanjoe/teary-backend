const core = require('./submissions.core');
const routes = require('./submissions.routes');
const storage = require('./submissions.storage');
const validation = require('./submissions.validation');

module.exports = {
  core,
  routes,
  storage,
  validation,
};
