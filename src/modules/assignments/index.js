const core = require('./assignments.core');
const routes = require('./assignments.routes');
const storage = require('./assignments.storage');
const validation = require('./assignments.validation');

module.exports = {
  core,
  routes,
  storage,
  validation,
};
