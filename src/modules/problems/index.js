const core = require('./problems.core');
const routes = require('./problems.routes');
const storage = require('./problems.storage');
const validation = require('./problems.validation');

module.exports = {
  core,
  routes,
  storage,
  validation,
};
