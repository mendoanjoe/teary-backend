const core = require('./roles.core');
const errors = require('./roles.errors');
const routes = require('./roles.routes');
const storage = require('./roles.storage');
const validation = require('./roles.validation');

module.exports = {
  core,
  errors,
  routes,
  storage,
  validation,
};
