const core = require('./role.core');
const errors = require('./role.errors');
const routes = require('./role.routes');
const storage = require('./role.storage');
const validation = require('./role.validation');

module.exports = {
  core,
  errors,
  routes,
  storage,
  validation,
};
