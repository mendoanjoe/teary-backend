const core = require('./file.core');
const errors = require('./file.errors');
const routes = require('./file.routes');
const validation = require('./file.validation');

module.exports = {
  core,
  errors,
  routes,
  validation,
};
